# NATS data-plane server config (per-region).
# Region-local JetStream for KV storage. No leaf nodes or federation —
# all cross-region communication uses HTTP.

listen: 0.0.0.0:4222

jetstream {
  store_dir: "/tmp/nats/jetstream"
}
