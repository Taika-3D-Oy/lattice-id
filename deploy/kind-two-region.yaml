kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # EU region gateway
      - containerPort: 30950
        hostPort: 8000
        protocol: TCP
      # US region gateway
      - containerPort: 30951
        hostPort: 8001
        protocol: TCP
