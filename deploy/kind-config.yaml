kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # Runtime gateway (HTTP traffic for workloads)
      - containerPort: 30950
        hostPort: 8000
        protocol: TCP
