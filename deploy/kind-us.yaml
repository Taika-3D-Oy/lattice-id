# Kind cluster config for US region (separate cluster per region).
# Gateway exposed on host port 8001.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # Runtime gateway (HTTP traffic) — port aligned with NodePort
      - containerPort: 8001
        hostPort: 8001
        protocol: TCP
kubeadmConfigPatches:
  - |
    kind: ClusterConfiguration
    apiServer:
      extraArgs:
        service-node-port-range: "8000-32767"
