package taika3d:lid;

interface authority {
    record lookup-result {
        found: bool,
        region: option<string>,
    }

    lookup: async func(email-hash: string) -> result<lookup-result, string>;
}
