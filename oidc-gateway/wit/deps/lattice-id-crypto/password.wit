package lattice-id:crypto;

interface password {
    /// Hash a plaintext password using argon2id. Returns a PHC-format string.
    hash: func(plain: string) -> result<string, string>;

    /// Verify a plaintext password against a PHC-format hash.
    verify: func(plain: string, phc-hash: string) -> result<bool, string>;
}
